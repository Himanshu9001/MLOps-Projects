{{- if .Values.rollout.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: churn-prediction-api
  namespace: {{ .Values.namespace }}
  labels:
    app: churn-prediction-api
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app: churn-prediction-api
  template:
    metadata:
      labels:
        app: churn-prediction-api
    spec:
      containers:
        - name: churn-prediction-api
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          env:
            - name: MLFLOW_TRACKING_URI
              valueFrom:
                secretKeyRef:
                  name: mlflow-secrets-csi
                  key: tracking-uri
            - name: APP_VERSION
              value: {{ .Values.canaryTest.version | quote }}
          volumeMounts:
            - name: secrets-store
              mountPath: "/mnt/secrets-store"
              readOnly: true
          resources:
            requests:
              memory: {{ .Values.resources.requests.memory | quote }}
              cpu: {{ .Values.resources.requests.cpu | quote }}
            limits:
              memory: {{ .Values.resources.limits.memory | quote }}
              cpu: {{ .Values.resources.limits.cpu | quote }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: 8000
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: 8000
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
      serviceAccountName: churn-prediction-sa
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "mlflow-secrets-provider"
  strategy:
    canary:
      # How many pods run the new version at each step
      maxSurge: 1
      maxUnavailable: 0
      # Reference to the AnalysisTemplate that checks Prometheus metrics
      analysis:
        templates:
          - templateName: churn-api-success-rate
        startingStep: 2       # Start analysis from step 2 onwards
      steps:
        - setWeight: 20       # 20% traffic to new version
        - pause:
            duration: 30s     # Wait 30 seconds
        - setWeight: 40       # 40% traffic to new version
        - pause:
            duration: 30s
        - setWeight: 60       # 60% traffic
        - pause:
            duration: 30s
        - setWeight: 80       # 80% traffic
        - pause:
            duration: 30s
        # Step 9: 100% — full promotion (automatic)
{{- end }}
