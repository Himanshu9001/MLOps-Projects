apiVersion: v1
kind: Service
metadata:
  name: churn-prediction-api
  namespace: churn-mlops
  labels:
    app: churn-prediction-api
spec:
  type: LoadBalancer
  selector:
    app: churn-prediction-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000